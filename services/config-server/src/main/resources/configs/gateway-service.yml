server:
  port: 8222

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: course-service
          uri: lb:http://COURSE-SERVICE
          predicates:
            - Path=/api/courses/**
